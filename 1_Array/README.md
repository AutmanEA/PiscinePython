# Piscine Python day 2

## file generator

to generate python files :
```bash
chmod +x generate_pyfile.sh
./generate_pyfile.sh
```
and then follow instructions (french only), first enter folder name, second enter file name.
